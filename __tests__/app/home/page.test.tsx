import React from 'react';
import HomePage from '@/app/home/page';

describe('HomePage Component', () => {
  it('exists', () => {
    // Basic test to ensure the component exists
    expect(HomePage).toBeDefined();
  });
});